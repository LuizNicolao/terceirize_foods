server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Configuração para assets estáticos
    location /implantacao/static/ {
        alias /usr/share/nginx/html/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Configuração para o frontend React
    location /implantacao {
        try_files $uri $uri/ /index.html;
    }

    # Configuração para a API do backend
    location /implantacao/api/ {
        proxy_pass http://implantacao_backend:3005/implantacao/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Prefix /implantacao;
    }

    # Fallback para SPA
    location / {
        try_files $uri $uri/ /index.html;
    }
}
