server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Configuração para assets estáticos
    location /cotacao/static/ {
        alias /usr/share/nginx/html/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Configuração para o frontend React
    location /cotacao {
        try_files $uri $uri/ /index.html;
    }

    # Configuração para a API do backend
    location /cotacao/api/ {
        proxy_pass http://cotacao_backend:5000/cotacao/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Prefix /cotacao;
    }

    # Fallback para SPA
    location / {
        try_files $uri $uri/ /cotacao/index.html;
    }
}
