# Configurações do Backend
NODE_ENV=development
PORT=5000
# IMPORTANTE: Use a mesma chave JWT do sistema principal para SSO funcionar
JWT_SECRET=foods_jwt_secret_key_2024

# SSO - Secret compartilhado entre Foods e Cotação (DEVE SER O MESMO DO FOODS)
SSO_SECRET=sso_shared_secret_2024_terceirizemais

# Configurações do Banco de Dados
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=cotacao_db
DB_PORT=3306

# Configurações do Frontend
REACT_APP_API_URL=http://localhost:5000 