/**
 * Índice das rotas de Saving
 * Centraliza a exportação das rotas organizadas
 */

const savingRoute = require('./savingRoute');
const savingValidator = require('./savingValidator');

module.exports = {
    savingRoute,
    savingValidator
};
