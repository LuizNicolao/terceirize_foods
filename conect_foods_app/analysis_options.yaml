# This file configures the analyzer to use the lint rule set from `package:flutter_lints`
# This rule set ensures that all of the recommended lints for Flutter apps are enabled.
# If you wish to remove any lint from this set, you can do so by modifying the `rules` section.

include: package:flutter_lints/flutter.yaml

# For more information about the core and recommended set of lints, see
# https://dart.dev/go/core-lints

# Uncomment to see all lints even those that cannot be auto-fixed by the analyzer
# show-fixes: true

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options 